entrypoint: bundle exec rackup --port $PORT
env: flex
runtime: ruby


env_variables:
    SECRET_KEY_BASE: ***REMOVED***

beta_settings:
  cloud_sql_instances: /cloudsql/***REMOVED***:us-central1:sql-instance
