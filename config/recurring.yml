default: &default
  # Temporarily disable the reports for this month
  # last_month_reports:
  #   command: "ReportMailer.last_month_csv_reports.deliver_later"
  #   priority: 1
  #   schedule: "every month at 9am, Australia/Adelaide"
# For quarterly
# every 3 months at 9am, Australia/Adelaide

development:
  <<: *default

production:
  <<: *default
