{
	"permissions": {
		"allow": [
			"WebFetch(domain:github.com)",
			"Bash(rake format)",
			"Bash(rake lint)",
			"Bash(bundle exec:*)"
		],
    "deny": [
      "Read(**/*.key)",
      "Read(**/*.enc)"
    ]
	}
}
